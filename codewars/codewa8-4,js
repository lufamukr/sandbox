'use strict';

function neutralise(s1, s2) {
    // Here be dragons!
    let sum = '';
    for(let i = 0; i < s1.length; i++) {
      if(s1[i] === s2[i]) {
        sum = sum + s1[i];
      } else {
        sum = sum + '0';
      }
    }
    return sum;
  }